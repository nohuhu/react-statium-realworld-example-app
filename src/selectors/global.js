// This is not a selector, just a constant. We need to keep it somewhere...
export const appName = 'Conduit';

export const getTab = ({ state }) => state.tab;
export const getPage = ({ state }) => state.page;
export const getLimit = ({ state }) => state.limit;
export const getSelectedTag = ({ state }) => state.selectedTag;
